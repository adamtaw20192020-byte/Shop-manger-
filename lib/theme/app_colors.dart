import 'package:flutter/material.dart';

/// Direct port of constants/colors.ts (light palette only, as in the
/// original scaffold).
class AppColors {
  static const background = Color(0xFFF4F0E6);
  static const foreground = Color(0xFF243126);

  static const card = Color(0xFFFFFDF7);
  static const cardForeground = Color(0xFF243126);

  static const primary = Color(0xFF3D6144);
  static const primaryForeground = Color(0xFFFFFFFF);

  static const secondary = Color(0xFFE9E2D2);
  static const secondaryForeground = Color(0xFF3D6144);

  static const muted = Color(0xFFEEE9DD);
  static const mutedForeground = Color(0xFF7B8178);

  static const accent = Color(0xFFDFD5B9);
  static const accentForeground = Color(0xFF3D6144);

  static const destructive = Color(0xFFEF4444);
  static const destructiveForeground = Color(0xFFFFFFFF);

  static const border = Color(0xFFD8D0BF);
  static const input = Color(0xFFC6BDA9);

  // Extra tones used inline in the original screens.
  static const amberBg = Color(0xFFFFF6E5);
  static const amberText = Color(0xFFC18421);
  static const amberDeep = Color(0xFFA86D13);
  static const amberDeeper = Color(0xFF8B6A2E);
  static const lowStockBorder = Color(0xFFEFC77F);
  static const lowStockBg = Color(0xFFF4EAD2);
  static const purchaseBg = Color(0xFFFFF3DF);
  static const purchaseText = Color(0xFFB77717);
  static const softBg = Color(0xFFEAF5ED);

  static const radius = 9.0;
}
